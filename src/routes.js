const HOME = "/";
const SIGN_UP = "/sign-up";
const LOGIN = "/login";
const LOGOUT = "/logout";

const routes = {
  home: HOME,
  login: LOGIN,
  signUp: SIGN_UP,
  logout: LOGOUT,
};

export default routes;
